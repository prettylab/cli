const warning = {
  NO_MODULES_FOUND: "No modules found in configuration.",
  DIRTY_WORKTREE:
    "Note: working tree has uncommitted changes (outside modules may also be dirty).",
};

export default warning;
