const error = {
  MISSING_CONFIG:
    "No pretty.json found. Run 'pretty init' in the root of your repository first.",
  NOT_IN_GIT_REPOSITORY: "Not inside a git repository.",
};

export default error;
